#include <stdio.h>
#include <string.h>

int main(void)
{

    //porque o ponteiro se desloca para frente(soma) ou
    //para trás(subtração) de acordo com o tipo da
    //variável que é declarada.
    //Se desloca em 4 no caso de int, 8 para double
    //e 1 para char

    return 0;
}

